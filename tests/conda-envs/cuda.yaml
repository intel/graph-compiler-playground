name: cuda
channels:
  - pytorch
  - nvidia
dependencies:
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
# The following packages are required to run benchmarks
  - sqlalchemy
  - pytest
