name: cpu
channels:
  - pytorch
dependencies:
  - python==3.11
  - pytorch
  - torchvision
  - torchaudio
  - cpuonly
# The following packages are required to run benchmarks
  - sqlalchemy
  - pytest
